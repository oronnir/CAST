Pre-request:
>pip install azure-cognitiveservices-vision-customvision
update the iris_scripts/config.py

(install other library like numpy,sklearn, matplotlib)

# Image classification sample example
1. Create image classification project and upload images.
example) python create_ic_project trainset_path dataset_name domain

2. Evaluate with published iteration
example) python predict_classification.py project_id iteration_id prediction_key testset_dictionary

3. Run end2end pipeline including create project, upload image, train project, publish iteration, prediction
example) python end2end_ic.py trainset_path testset_path dataset_name domain
 e.g. python end2end_ic.py classification_datasets\soda\train classification_datasets\soda\test soda_demo General

output:
print out confusion matrix
plot PR curve with ap


# Object detection sample
How to use these scripts:

Setup:
>pip install azure-cognitiveservices-vision-customvision

How to prepare a dataset. For the detail of the format, please read the comments in the scripts.

1. Create a project.
example) python create_od_project.py detection_datasets/animal661/train_images.txt detection_datasets/animal661/labels.txt

2. Evaluate the project.
example) python predict_od.py <project_id> <iteration_id> detection_datasets/animal661/test_images.txt detection_datasets/animal661/labels.txt

3. Run end2end pipeline including create project, upload image, train project, publish iteration, prediction
example) python end2end_od.py detection_datasets\animal661\train_images.txt detection_datasets\animal661\labels.txt detection_datasets\animal661\test_images.txt General